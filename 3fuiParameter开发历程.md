# 3fuiParameter - 开发历程

## 项目背景

在使用 FFmpegFreeUI (3FUI) 进行视频转码时，我需要将各种不同分辨率的视频统一
转换为 16:9 比例，并缩放至 1280x720。核心操作是在 FFmpeg 滤镜参数中填写
crop（裁剪）和 scale（缩放）命令。

然而，在填写裁剪参数时频繁出错。最典型的问题来自 crop 滤镜的计算方式：
`crop=宽:高`
其中「宽」和「高」是裁剪后保留的区域大小。要裁剪为 16:9 比例，需要根据原视频
分辨率判断哪个维度是「短板」，然后以短板为基准计算另一个维度。

最初我尝试使用 `crop=iw:iw*16/9`，结果 FFmpeg 报错，提示高度超出原视频范围。
后来才理解：对于 16:9 的画面，高度 = 宽度 × 9/16，而不是宽度 = 高度 × 16/9。

但不同的视频来源分辨率各异，每次都需要手动判断：

- 如果视频偏窄（如 1880x1080），需要保持宽度不变，裁剪高度
- 如果视频偏宽（如 2560x1080），需要保持高度不变，裁剪宽度

反复手动计算既繁琐又容易出错，于是决定开发一个辅助工具，自动完成检测和参数生成。

## 核心逻辑提炼

经过多次尝试和总结，确定了裁剪参数的核心判断规则：

> 哪个维度是「短板」（即数值更小、不够用），就把它作为基准放在等号后面，
> 然后用目标比例计算另一个维度。

以目标比例 16:9 为例：

1. 如果宽度是短板（视频偏窄）：
   `crop=iw:iw*9/16`
   含义：宽度保持不变，高度 = 宽度 × 9/16

2. 如果高度是短板（视频偏宽）：
   `crop=ih*16/9:ih`
   含义：高度保持不变，宽度 = 高度 × 16/9

这个逻辑后来被固化为程序的核心判断算法。

## 工具功能实现

基于上述逻辑，使用 Python + PySide6 开发了本工具，实现了以下功能：

1. 批量检测视频文件的分辨率、平均码流、色彩空间（SDR/HDR）
2. 自动判断每个视频属于「符合比例」「宽度不够」「高度不够」三类
3. 为每类文件自动生成对应的 FFmpeg 裁剪+缩放滤镜参数
4. 支持按分类将文件移动到对应的文件夹中
5. 支持添加单个文件或整个文件夹，自动去重
6. 所有信息以表格形式清晰展示

## 技术选型

- GUI 框架：PySide6（Qt for Python）
- 视频信息读取：ffprobe（FFmpeg 工具集）
- 文件操作：Python 标准库 (os, shutil, subprocess)
- 数据类型：5 列信息（文件名不含扩展名、扩展名、分辨率、码流、色彩空间）

## 遇到的问题与解决

1. **ffprobe 输出格式不统一**
   不同视频的元数据字段数量和顺序不同，有的有色彩信息，有的没有。
   解决：对 ffprobe 输出进行智能解析，根据字段内容自动识别数据类型。

2. **界面表格对齐**
   纯文本方式在 QTextEdit 中对齐困难，且不同字体下显示效果不同。
   解决：使用 HTML 表格 + CSS 固定列宽 + 等宽字体 (Consolas)。

3. **参数联动计算**
   用户修改裁剪比例时，需要自动计算目标分辨率并向上取整为偶数。
   解决：通过信号‑槽机制实现双向联动，使用 math.ceil 向上取整。

4. **防止命令行窗口闪烁**
   subprocess 调用 ffprobe 时默认会弹出 CMD 窗口。
   解决：添加 creationflags=CREATE_NO_WINDOW 参数。

## 最终成果

本工具已稳定运行，能够快速处理大批量视频文件，自动生成准确的 FFmpeg 滤镜参数，
大幅减少了手动计算和参数填写错误，提高了视频处理效率。

项目已开源在 GitHub，欢迎使用和反馈。
