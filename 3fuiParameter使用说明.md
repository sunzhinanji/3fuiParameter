# 3fuiParameter - 使用说明

## 软件简介

本工具是一个基于 PySide6 和 FFmpeg 的 GUI 辅助程序，用于批量检测视频文件的
分辨率、平均码流和色彩空间（SDR/HDR），并根据用户设定的目标比例自动分类，
生成对应的 FFmpeg 裁剪（crop）和缩放（scale）滤镜参数。

## 系统要求

- Windows 操作系统（已测试 Windows 10/11）
- FFmpeg 已安装并添加到系统 PATH 环境变量
- Python 3.8+（如直接运行源码）
- 依赖库：PySide6

## 安装方法

### 方法一：直接运行（需要 Python 环境）

1. 安装 Python 3.8+
2. 安装依赖：`pip install PySide6`
3. 下载 3fuiParameter.pyw 并双击运行

### 方法二：打包为 EXE（可选）

使用 PyInstaller 打包：
pyinstaller --onefile --windowed --name "3fuiParameter" 3fuiParameter.py

## 操作步骤

### 第一步：设置目标参数

在顶部输入框中设置：

- 「裁剪比例」：目标宽高比，默认为 16:9
- 「宽:高」：目标输出分辨率，默认为 1280:720

两参数双向联动，修改任一参数会自动计算另一个。

### 第二步：添加素材

点击「添加文件」选择单个或多个视频文件
或点击「添加文件夹」选择包含视频的文件夹（自动递归扫描）

### 第三步：查看检测结果

工具自动检测每个视频的：

- 分辨率（宽×高）
- 平均码流（自动显示 Mbps 或 kbps）
- 色彩空间（SDR 或 HDR）

并按比例自动分为三类显示在左侧表格中。

### 第四步：复制滤镜参数

右侧三个区域分别对应三类文件：

- 白色（符合比例）：无需裁剪，直接缩放
- 绿色（宽度不够）：需裁剪宽度
- 蓝色（高度不够）：需裁剪高度

点击「复制参数且打开文件夹」即可复制对应的 FFmpeg 滤镜参数，
并自动打开文件所在文件夹。

### 第五步：分类素材（可选）

点击「分类素材」按钮，工具会自动：

- 将「宽度不够」的文件移动到 nWe(green) 文件夹
- 将「高度不够」的文件移动到 nTe(blue) 文件夹
- 「符合比例」的文件保留在原位置不动

## FFmpeg 滤镜参数说明

三类文件对应的滤镜参数：

1. **符合比例** → `scale=1280:720`
   含义：直接缩放，无需裁剪

2. **宽度不够** → `crop=iw:iw*9/16,scale=1280:720`
   含义：宽度保持不变，高度裁剪为宽度的 9/16，再缩放到目标分辨率

3. **高度不够** → `crop=ih*16/9:ih,scale=1280:720`
   含义：高度保持不变，宽度裁剪为高度的 16/9，再缩放到目标分辨率

将生成的参数复制到 FFmpegFreeUI 的「自定义视频滤镜」框中即可使用。

## 支持的视频格式

本工具通过 ffprobe 检测视频信息，支持 FFmpeg 能处理的所有视频格式：
.mp4, .mkv, .avi, .mov, .flv, .wmv, .mpeg, .ts, .3gp, .webm,
.h264, .h265, .hevc, .av1 等数十种格式。

## 常见问题

**Q：点击「添加文件」后没有反应？**
A：请检查 FFmpeg 是否正确安装并已添加到 PATH。可在 CMD 中执行
`ffprobe -version` 验证。

**Q：检测结果中「色彩空间」显示为 SDR，但视频实际上是 HDR？**
A：本工具根据 color_primaries 和 color_trc 元数据判断，部分视频
可能缺少元数据，导致无法正确识别。

**Q：分类素材后文件找不到了？**
A：文件被移动到原文件夹下的子文件夹中：

- nWe(green)  ← 宽度不够的文件
- nTe(blue)   ← 高度不够的文件
  符合比例的文件仍保留在原位置。

**Q：如何撤销误操作？**
A：点击「清空列表」后，可点击「撤销清空」恢复所有数据。
只保留最近一次清空的备份。

## 开源协议

MIT License

## 联系方式

如有问题或建议，欢迎提交 Issue 或 Pull Request。
